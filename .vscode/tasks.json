{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Cargo debug build",
            "command": "cargo",
            "args": ["build"],
            "options": {
                "cwd": "${workspaceFolder}"
            }
        }
    ]
}
